[filter "lfs"]
        process = git-lfs filter-process
        required = true
        clean = git-lfs clean -- %f
        smudge = git-lfs smudge -- %f
[user]
        name = Vittoria Bitton
        email = bitton.vittoria@gmail.com

[includeIf "gitdir:~/Workspace/valued/**/.git"]
  path = ~/.gitconfig-valued
